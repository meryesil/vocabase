# Project Rules & Customizations

- **NEVER automatically run `git commit` or `git push`**: The user explicitly requested to review all code updates and changes locally first. Do not commit or push changes to GitHub or any remote repository unless the user explicitly asks you to run a git commit/push. Leave all modified files unstaged/uncommitted so the user can test locally and push at their own discretion.
